# Webots e-puck Obstacle + Wall Following: Full Source

## `main.lisp`

```lisp
--8<-- "examples/webots_epuck_obstacle/lisp/main.lisp"
```

## `bt_obstacle_wallfollow.lisp`

```lisp
--8<-- "examples/webots_epuck_obstacle/lisp/bt_obstacle_wallfollow.lisp"
```

## Walkthrough

- `main.lisp` sets runtime options, log handling, and execution loop structure.
- `bt_obstacle_wallfollow.lisp` contains branch logic for obstacle handling and wall-following.
- Combined traces + logs explain branch switching events over time.
