# navigation capability mock

This example shows the `cap.navigation.v1` request shape for a future Nav2-backed adapter.

It uses the built-in deterministic mock adapter named `mock-nav2`. It does not require ROS2 or Nav2.

Run:

```bash
./build/core-only/muslisp examples/capabilities/nav2_navigation/mock_nav_goal.lisp
```
